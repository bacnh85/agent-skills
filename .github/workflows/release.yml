name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Pack and inspect tarball
        id: pack
        run: |
          npm pack
          TARBALL=$(ls bacnh85-agent-skills-*.tgz)
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"
          echo "--- tarball contents ---"
          tar -tzf "$TARBALL"

      - name: Verify global install from tarball
        run: |
          npm install -g "${{ steps.pack.outputs.tarball }}"
          agent-skills --help

      - name: Verify project-local install
        run: |
          cd "$(mktemp -d)"
          npm install "$GITHUB_WORKSPACE/${{ steps.pack.outputs.tarball }}"
          npx agent-skills --help

      - name: Check if version is already published
        id: check
        run: |
          VERSION=$(node -e "console.log(require('./package.json').version)")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if npm view "@bacnh85/agent-skills@$VERSION" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "Version $VERSION is already published, skipping npm publish."
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.check.outputs.published == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is not configured in GitHub repository settings."
            echo "::error::Go to https://github.com/bacnh85/agent-skills/settings/secrets/actions"
            echo "::error::and create a new secret named NPM_TOKEN with your npm automation token."
            exit 1
          fi
          echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}" > .npmrc
          npm whoami
          npm publish --provenance --access public

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
